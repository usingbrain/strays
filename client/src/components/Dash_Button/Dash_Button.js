import React from 'react';

function Dash_Button({ text }) {
  return (
    // on click change state to be <clicked>
    <div>
      <button>{text}</button>
    </div>
  );
}

export default Dash_Button;
